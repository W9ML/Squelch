[Service]
# systemd watchdog: the app pings WATCHDOG=1 while the pipeline is healthy
# (squelch/watchdog.py). A wedged event loop or a sustained GPU/CUDA fault stops
# the pings; systemd then kills and (Restart=always) restarts a fresh process.
WatchdogSec=180
